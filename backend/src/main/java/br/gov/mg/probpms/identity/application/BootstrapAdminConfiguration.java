package br.gov.mg.probpms.identity.application;

/**
 * ⚠️ DEPRECATED - Não mais utilizado
 * Autenticação é gerenciada EXCLUSIVAMENTE pelo Supabase.
 * Não crie usuários localmente - use o Supabase Dashboard ou API.
 */
@Deprecated(forRemoval = true)
public class BootstrapAdminConfiguration {
  // Removed - use Supabase Authentication
  }System.out.println("✓ Admin bootstrap user created: "+email);}}}catch(
  Exception ex)
  {
        System.out.println("⚠ Bootstrap admin creation skipped: " + ex.getMessage());
      }
};}}
